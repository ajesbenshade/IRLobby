This folder contains DigitalOcean App Platform configuration.

Use `.do/app.yaml` to force using the Dockerfile and prevent Node.js autodetection from running a second build.

During App creation, choose "Use existing app spec" and point to `.do/app.yaml`.