{
  "name": "irlobby-workspace",
  "private": true,
  "scripts": {
    "check:api-contract": "node scripts/check-api-contract.mjs",
    "check:web:lint": "npm --prefix apps/web run lint",
    "check:web": "npm --prefix apps/web run test && npm --prefix apps/web run build",
    "check:mobile": "npm --prefix apps/mobile run typecheck && npm --prefix apps/mobile run bundle:ios",
    "check:release": "npm run check:api-contract && npm run check:web && npm run check:mobile"
  }
}
